---
phase: 22-postgres-foundation
plan: 04
type: execute
---

<objective>
Get the solution to **compile green** against PostgreSQL by replacing every `Microsoft.Data.SqlClient`
API usage in the data layer (`SqlParameter`, `SqlConnection`, `SqlCommand`, `SqlDataAdapter`,
`SqlDataReader`, `SqlDbType`) — found in **51 files** — with the Npgsql / PetaPoco equivalent. This is
the API-level swap; making the *SQL text* correct (T-SQL → PG dialect) is Phase 23, not here.

Purpose: 22-03 wired the provider but left ~51 files referencing the SQL Server client API, so the
build is red. This plan makes it green so the foundation is a buildable baseline.
Output: zero `Microsoft.Data.SqlClient` references in `LabBilling Library` (and dependent projects);
solution builds with VS MSBuild, 0 errors.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-postgres-foundation/22-03-SUMMARY.md

Scope: 51 files under `LabBilling Library` reference the SqlClient API (full list via
`grep -rl "Microsoft.Data.SqlClient\|SqlParameter\|SqlConnection\|SqlDataAdapter\|SqlDbType" "LabBilling Library"`).
Most are `using Microsoft.Data.SqlClient;` + typed `SqlParameter` for PetaPoco args. Also check
`Utilities`, `LabBillingService`, `LabBillingConsole`, `Lab Patient Accounting Job Scheduler` for
SqlClient usage that breaks once the package is removed from `LabBilling Core`.

Build with VS MSBuild (`/p:Platform=x64`) — NOT `dotnet build` (Utilities COM ref). Latest stable
packages only (no prerelease).

IMPORTANT line between 22 and 23: this plan changes API TYPES so the code compiles. It does NOT
rewrite T-SQL strings (`TOP`, `[brackets]`, `dbo.GetAccBalance(...)`, `;EXEC GetNextNumber ... OUTPUT`,
raw `SqlConnection`/`SqlDataAdapter` in `ReportingRepository`) into PostgreSQL dialect — those are
Phase 23-03 and may still fail at RUNTIME. Compiling green is the bar here.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace SqlParameter / SqlDbType usages with PetaPoco-native or Npgsql params</name>
  <files>LabBilling Library/Repositories/*.cs (the subset using SqlParameter/SqlDbType), LabBilling Library/Services/*.cs</files>
  <action>
    For each site, prefer the PetaPoco-native parameter style (anonymous args / `@0` positional) over
    a provider-specific parameter object, since PetaPoco abstracts the provider — e.g. replace
    `new SqlParameter() { SqlDbType = SqlDbType.VarChar, Value = key }` passed to
    `SingleOrDefault<T>("where name = @0", param)` with simply `SingleOrDefault<T>("where name = @0", key)`.
    Where a provider parameter object is genuinely needed, use `NpgsqlParameter`/`NpgsqlDbType`.
    Update the `using` directives (`Microsoft.Data.SqlClient` → `Npgsql` or remove). Do this across
    the repositories/services in the 51-file set. Do NOT alter the SQL string contents themselves
    (that's Phase 23) — only the parameter/type API.
  </action>
  <verify>`grep -rl "SqlParameter\|SqlDbType\|Microsoft.Data.SqlClient" "LabBilling Library"` returns zero files; the SQL strings are unchanged (diff shows only param/using changes).</verify>
  <done>All SqlParameter/SqlDbType API usages in LabBilling Library are PetaPoco-native or Npgsql; no SQL text rewritten.</done>
</task>

<task type="auto">
  <name>Task 2: Handle raw ADO.NET sites + remove the SqlClient package, build green</name>
  <files>LabBilling Library/Repositories/ReportingRepository.cs, LabBilling Library/LabBilling Core.csproj, (Utilities/service/console csproj as needed)</files>
  <action>
    `ReportingRepository.cs` uses raw `SqlConnection`/`SqlDataAdapter` (bypasses PetaPoco) — convert
    to `NpgsqlConnection`/`NpgsqlDataAdapter` (or a PetaPoco fetch) so it COMPILES; leave the SQL text
    for Phase 23-03 (flag any obviously-T-SQL query in a comment). Remove the
    `Microsoft.Data.SqlClient` PackageReference from `LabBilling Core.csproj` once no code references
    it. If `Utilities`/service/console projects reference SqlClient, resolve those too (swap or keep
    the package only where genuinely still needed and document why). Then build the whole solution
    with VS MSBuild and drive to **0 errors** (warnings ok). Iterate build→fix.
  </action>
  <verify>Full-solution VS MSBuild (`/p:Platform=x64 /t:Build`) exits 0 with 0 errors; `Microsoft.Data.SqlClient` is removed from `LabBilling Core.csproj` (and elsewhere unless justified).</verify>
  <done>The solution compiles green against Npgsql with no SqlClient references in the data layer.</done>
</task>

</tasks>

<verification>
- [ ] Zero `Microsoft.Data.SqlClient` / `SqlParameter` / `SqlDbType` references in `LabBilling Library`
- [ ] Full solution builds with VS MSBuild: 0 errors
- [ ] SQL string contents unchanged (T-SQL→PG dialect deferred to Phase 23-03) — verify via diff
- [ ] No prerelease packages introduced
</verification>

<success_criteria>
- The solution is a green-building PostgreSQL/Npgsql baseline.
- Remaining SQL-correctness work is cleanly isolated to Phase 23 (documented).
</success_criteria>

<output>
Create `.planning/phases/22-postgres-foundation/22-04-SUMMARY.md` with: files touched count, the SqlClient sites that needed real refactors (ReportingRepository etc.), any project still legitimately on SqlClient, and "Ready for 22-05-PLAN.md (smoke verification)".
</output>
