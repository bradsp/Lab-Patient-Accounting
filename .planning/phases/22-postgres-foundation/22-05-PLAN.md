---
phase: 22-postgres-foundation
plan: 05
type: execute
---

<objective>
Prove the foundation end-to-end: with the schema on PostgreSQL and the data layer on Npgsql, seed the
minimum data, point `LabOutreachUI` at PostgreSQL, and confirm it boots, logs in, and performs
auto-CRUD reads/writes through PetaPoco on tables that carry **no** hand-written T-SQL (those are
Phase 23). This is the Phase 22 acceptance gate.

Purpose: closes Phase 22 by demonstrating the app runs on PostgreSQL for the simple-repository paths,
isolating the remaining failures to the known Phase 23 SQL-dialect work.
Output: app running against PostgreSQL with a passing smoke check; Phase 22 marked complete.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
@~/.claude/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-postgres-foundation/22-04-SUMMARY.md
@Database/Seed/seed-min-data.sql

Environment facts (this session):
- Local PG17: container `labpa_postgres`, host `localhost:5434`, db `labbilling`, user `labpa`.
- `LabOutreachUI` reads `AppSettings` from `appsettings.json` (+ `appsettings.Development.json` which
  sets `DevelopmentUser`). The existing SQL Server seed is `Database/Seed/seed-min-data.sql` (188
  `system` params + `emp` users; note: on the SQL Server side login resolved to `WTHMC\bpowers`→`bpowers`).
- Run the built app from `LabOutreachUI/bin/Debug/net10.0/` with `ASPNETCORE_ENVIRONMENT=Development`;
  copy the current `appsettings.json` into the output dir first (build copy goes stale). App listens
  on https://localhost:7063 (use `curl -k`). Build with VS MSBuild.
- `dictionary.emp` needed an `access_random_drug_screen` column on SQL Server (model drift) — ensure
  the PostgreSQL schema includes it (it should, if translated from canonical).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the minimal seed to PostgreSQL and point the app at PG</name>
  <files>Database/Seed/seed-min-data.pg.sql, LabOutreachUI/appsettings.json</files>
  <action>
    Translate `Database/Seed/seed-min-data.sql` into PostgreSQL (`seed-min-data.pg.sql`): T-SQL →
    PG syntax (identifier quoting, `GETDATE()`→`now()`, the SHA256 password hash via `digest()`/
    `encode()` from `pgcrypto` or a precomputed literal, `IF NOT EXISTS`→`ON CONFLICT DO NOTHING`).
    Seed the 188 `system` params + the `emp` users (include the dev login identity — `bpowers` and a
    portable `admin`). Apply to `labbilling`. Update `LabOutreachUI/appsettings.json` `AppSettings`
    to point at the PG instance (Host `localhost`, Port `5434`, Database `labbilling`, Username/
    Password, `SslMode` dev value, `AuthenticationMode=SqlServer`-equivalent SQL-auth). Keep this
    local-only/uncommitted (secrets) consistent with prior practice.
  </action>
  <verify>`select count(*) from "system"` and `emp` return the seeded rows in PostgreSQL; appsettings points at localhost:5434/labbilling.</verify>
  <done>PostgreSQL holds the minimal seed and the app config targets PostgreSQL.</done>
</task>

<task type="auto">
  <name>Task 2: Boot the app and run an auto-CRUD smoke check</name>
  <files>(no repo files — runtime verification; capture results in the SUMMARY)</files>
  <action>
    Rebuild `LabOutreachUI` (VS MSBuild), copy the fresh `appsettings.json` to the output dir, run
    with `ASPNETCORE_ENVIRONMENT=Development`, capture logs. Confirm: app connects to PostgreSQL
    (no connection/`LoadSystemParameters` failure), `GET /` returns 200 (login as the seeded dev
    user passes the DatabaseUser policy via `emp`). Then exercise a couple of **auto-CRUD** repos
    that use plain PetaPoco (no hand-written T-SQL) — e.g. read a dictionary table and round-trip an
    insert/update/delete on a safe table — to confirm PetaPoco's PG provider does CRUD + `RETURNING`
    key retrieval. Record which repos/paths work and which throw the expected Phase-23 SQL-dialect
    errors (`TOP`, inline UDFs, `EXEC GetNextNumber`). Stop the app afterward.
  </action>
  <verify>App boots against PostgreSQL; `GET /` = 200; at least one auto-CRUD insert/update/delete round-trips with `RETURNING`-based key retrieval; failures are limited to known T-SQL-dialect sites (catalogued).</verify>
  <done>The app runs on PostgreSQL for simple-repository paths; remaining failures are isolated to the documented Phase 23 SQL work.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>LabOutreachUI running against PostgreSQL — connects, logs in, and does auto-CRUD through PetaPoco's PG provider.</what-built>
  <how-to-verify>
    1. With the app running (Development), hit https://localhost:7063/ (`curl -k`) → 200.
    2. Review the smoke-check log: connection OK, system params loaded, login authorized, one CRUD round-trip succeeded.
    3. Confirm the only failures are the known Phase-23 T-SQL-dialect paths (not connection/provider failures).
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 22 complete, or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- [ ] PostgreSQL seeded (system + emp); app config targets PG
- [ ] App boots against PostgreSQL; `GET /` = 200; login works
- [ ] An auto-CRUD repo round-trips insert/update/delete with RETURNING key retrieval
- [ ] Remaining failures catalogued and limited to Phase 23 SQL-dialect sites
- [ ] Phase 22 marked Complete in ROADMAP.md
</verification>

<success_criteria>
- The app demonstrably runs on PostgreSQL for the simple data-access paths — the foundation is real, not theoretical.
- A clean hand-off to Phase 23 (logic & repo port): the exact list of SQL-dialect sites still to fix.
</success_criteria>

<output>
Create `.planning/phases/22-postgres-foundation/22-05-SUMMARY.md` with: boot/login/CRUD results, the catalogued Phase-23 failure sites, and "Phase 22 complete — ready to detail-plan Phase 23". Update ROADMAP Phase 22 → Complete (5/5).
</output>
