---
phase: 22-postgres-foundation
plan: 01
type: execute
---

<objective>
Stand up a local PostgreSQL 17 container and load the **canonical** SQL Server schema into a fresh
database so Phase 22-02 translates from a complete, production-grade source — not the drifted
`LabBillingTest` dev snapshot (risk R3).

Purpose: the migration foundation needs both endpoints in place — a target PostgreSQL and a complete
source schema. The repo's canonical schema is `LabPatientAccounting DB Build.sql` (4.3 MB) and the
`LabBillingDatabase` SQL Server Database Project; the running `LabBillingTest` is an older partial
snapshot and must not be the translation source.
Output: a `postgres:17` service in `docker-compose.yml` (running), and a fresh SQL Server DB
(`LabBillingCanonical`) on the existing `mssql_server` container loaded from the canonical script.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/21-derisking-spikes/FINDINGS.md
@docker-compose.yml
@CLAUDE.md

Environment facts (verified this session):
- Existing docker SQL Server: container `mssql_server`, host `localhost:1433`, SA password
  `YourStrong!Passw0rd`. The dev DB `LabBillingTest` is a drifted snapshot — DO NOT translate from it.
- Run sqlcmd inside the container via the PowerShell tool (Git Bash mangles `/opt/...`):
  `docker exec mssql_server /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P '...' -C -i <script>`
- A different project's Postgres runs on host port 5433 — **avoid that port**; use 5434 for this one.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a PostgreSQL 17 service to docker-compose and bring it up</name>
  <files>docker-compose.yml</files>
  <action>
    Add a `postgres` service: image `postgres:17`, container_name `labpa_postgres`, restart
    unless-stopped, host port **5434**→5432 (5433 is taken by another project), a named volume
    `pgdata:/var/lib/postgresql/data`, and env `POSTGRES_DB=labbilling`, `POSTGRES_USER=labpa`,
    `POSTGRES_PASSWORD=<dev pw>`. Add the `pgdata` volume to the volumes section. Do NOT remove or
    alter the existing `mssql` service. `docker compose up -d postgres`, then verify with
    `docker exec labpa_postgres psql -U labpa -d labbilling -c "select version();"` (expect PostgreSQL 17.x).
  </action>
  <verify>`docker ps` shows `labpa_postgres` healthy on 5434; the `select version()` returns PostgreSQL 17.x.</verify>
  <done>A PostgreSQL 17 container is running locally on port 5434 with an empty `labbilling` database, and docker-compose still starts the existing mssql service.</done>
</task>

<task type="auto">
  <name>Task 2: Load the canonical SQL Server schema into a fresh DB</name>
  <files>(no repo files — container state only)</files>
  <action>
    Create a fresh database `LabBillingCanonical` on `mssql_server` and run the canonical
    `LabPatientAccounting DB Build.sql` against it (via `sqlcmd -i`, copying the script into the
    container or mounting it). The script is large — if it errors partway, capture the failing
    batch, note it, and proceed as far as the table/constraint DDL (procs/functions/views failing is
    acceptable here — Phase 22 only needs the TABLE structure; procs/views are deferred to Phase 23).
    The goal is a DB whose TABLE catalog matches production. Verify the table count is in the
    expected ~500 range: `SELECT COUNT(*) FROM LabBillingCanonical.sys.tables;`.
    If the build script proves impractical to load wholesale, fall back to extracting just the
    `CREATE TABLE`/constraint/index DDL from the `LabBillingDatabase` DB project's `.sql` files and
    load that subset — document whichever path was taken.
  </action>
  <verify>`SELECT COUNT(*) FROM LabBillingCanonical.sys.tables` returns a count materially larger than LabBillingTest's 163 (target ~500); the load path (full script vs DB-project DDL subset) is documented in the SUMMARY.</verify>
  <done>A complete-as-practical canonical SQL Server schema exists in `LabBillingCanonical`, suitable as the pgloader source for 22-02, with the table count recorded.</done>
</task>

</tasks>

<verification>
- [ ] `docker-compose.yml` adds a `postgres:17` service (port 5434) without disturbing the mssql service
- [ ] PostgreSQL 17 container is up and reachable
- [ ] `LabBillingCanonical` exists on the SQL Server with a ~500-table catalog (count recorded)
- [ ] No change to the running `LabBillingTest` DB
</verification>

<success_criteria>
- Both translation endpoints exist: an empty local PostgreSQL 17, and a complete canonical SQL Server schema source.
- Phase 22-02 can run pgloader from `LabBillingCanonical` → the new PostgreSQL without further setup.
</success_criteria>

<output>
Create `.planning/phases/22-postgres-foundation/22-01-SUMMARY.md`:

# Phase 22 Plan 01: PostgreSQL + Canonical Schema Source Summary
**[One-liner: e.g. "PG17 up on 5434; LabBillingCanonical loaded with N tables from the build script."]**
## Accomplishments
- PostgreSQL 17 container (port, db/user)
- Canonical source DB table count + load path used
## Files Created/Modified
- `docker-compose.yml` — postgres service
## Decisions Made / Issues Encountered
[build-script load issues, fallback to DB-project DDL, etc., or None]
## Next Step
Ready for 22-02-PLAN.md (table-schema translation).
</output>
