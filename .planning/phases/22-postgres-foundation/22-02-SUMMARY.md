# Phase 22 Plan 02: Table-Schema Translation Summary

**Full SQL-Server→PostgreSQL table-schema translation: 270/270 tables, 3959/3959 columns, applies clean from scratch (0 errors) — verified parity, approved.**

## Accomplishments
- pgloader translation `LabBillingCanonical` (SQL Server) → `labbilling` (PostgreSQL 17, port 5434), tables only.
- Committed, repeatable DDL: `schema-tables.sql` (270 `CREATE TABLE`) + `schema-fixups.sql` (SQL-Server-ism corrections). Apply order: tables then fixups; both `ON_ERROR_STOP=1`, exit 0.
- **Parity (human-verified):** tables 270/270 (per-schema exact: dbo 224, audit 25, dictionary 7, infce 5, dict 4, zzz 4, tst 1), columns 3959/3959, PKs 138/138, identity 80/80. Views/procs = 0 (deferred to Phase 23).

## Files Created/Modified
- `Database/Postgres/pgloader.load`
- `Database/Postgres/schema-tables.sql`
- `Database/Postgres/schema-fixups.sql`
- `.planning/ROADMAP.md` — Phase 22 → 2/5

## Decisions Made
- **Identifier policy (affects 22-03/04): preserve original SQL Server casing; all identifiers double-quoted** — PG names are byte-identical to the .NET `[TableName]`/`[Column]` attributes. 22-03/04 must use PetaPoco's PG provider quoting and apply NO lowercasing/name-mangling.
- **Numeric IDENTITY columns (27, e.g. `chrg.chrg_num numeric(15,0)`):** kept `numeric` (type parity) + owned bigint sequence default rather than `GENERATED AS IDENTITY` (which requires an integer type). Int identities (53) → `GENERATED BY DEFAULT AS IDENTITY`.
- **Computed columns:** 3 deterministic → `GENERATED ALWAYS AS (...) STORED` (`insc.fin_class`, `tempChk.total`, `tempDaily.error`); 2 kept as PLAIN columns and flagged for Phase 23 — `chrg.calc_amt` (needs un-ported UDF `dbo.GetAmountTotal`) and `chrg.age_on_date_of_service` (non-deterministic `getdate()`/`datediff`).
- **Types:** datetime/datetime2→timestamp, bit→boolean, money→numeric(19,4), uniqueidentifier→uuid (+uuid-ossp), varbinary→bytea, xml→xml, nvarchar(max)→text, char/nchar→varchar (avoid blank-padding); `getdate()`→`now()`.

## Issues Encountered
- pgloader `--network host` doesn't bridge sibling containers on Docker Desktop → used `host.docker.internal`; `TDS_MAX_CONN=512` to silence a TDS warning.
- T-SQL text defaults (`app_name()`/`suser_sname()`/literal+CONVERT) copied verbatim by pgloader (invalid PG) → dropped on text types in CAST rules; ~36 meaningful literal/`now()` defaults re-applied in fixups. Audit `mod_*` server-side defaults intentionally not restored (the .NET app supplies them).
- xml btree index on `infce.patient_demographics(xmlContents)` → recreated as a functional index on `(xmlContents::text)`.
- Minor: a few numeric typemods lost precision in pgloader output (`chrg.qty` → bare `numeric`); no impact on parity/binding; flagged for Phase 23 if exactness matters.
- Note: source has **7 schemas** (not 11 as 22-01 SUMMARY stated), incl. `tst`/`zzz` scratch schemas (harmless, carried over).

## Next Step
Ready for 22-03-PLAN.md (provider + connection swap to Npgsql / PetaPoco PG provider).
