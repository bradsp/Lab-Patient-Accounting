---
phase: 13-dict-clients
plan: 02
type: execute
---

<objective>
Build the Client Maintenance LIST page at `/dictionary/clients`: a searchable, filterable table of
clients with an "Add New" action and per-row navigation to the edit page. This mirrors the legacy
`ClientMaintenanceForm` (the list/grid form), not the edit dialog.

Purpose: Give dictionary editors a browseable entry point to all clients, matching the WinForms grid
(filter by name/mnemonic, toggle deleted, add new), before the edit form is built in 13-03.
Output: `Pages/Dictionary/ClientMaintenance.razor` — the list page reachable from the nav link.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/13-dict-clients/13-01-SUMMARY.md
@LabOutreachUI/Pages/Clients/ClientList.razor
@LabOutreachUI/_Imports.razor

**Key facts already verified (do not re-derive):**
- The read-only `Pages/Clients/ClientList.razor` (route `/clients`) is the closest existing pattern:
  it `@inject DictionaryService DictionaryService`, `@inject LabBilling.Core.UnitOfWork.IUnitOfWork UnitOfWork`,
  `@inject NavigationManager NavigationManager`, and loads via
  `await DictionaryService.GetAllClientsAsync(UnitOfWork, showInactive)` returning `List<Client>`.
- `_Imports.razor` already includes `LabBilling.Core.Models` and `LabBilling.Core.Services`, so `Client`
  and `DictionaryService` need no extra @using.
- `DictionaryService` and `IUnitOfWork` are registered scoped in `Program.cs`.
- UI is Bootstrap 5 + Open Iconic (`oi oi-*`); no MudBlazor/QuickGrid. Use `<table class="table table-sm ...">`.
- Legacy grid columns (ClientMaintenanceForm): ClientMnem, Name, StreetAddress1, StreetAddress2, City,
  State, ZipCode, FacilityNo, Type, BillMethod. Deleted rows shown greyed; hidden unless "include deleted".
- Legacy filter: free-text matches Name (contains, case-insensitive) OR ClientMnem (exact, case-insensitive),
  debounced ~650ms.
- `Client` properties available: `ClientMnem, Name, StreetAddress1, StreetAddress2, City, State, ZipCode,
  FacilityNo, Type (int), BillMethod, IsDeleted, ClientType (nav, may be null)`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Client Maintenance list page shell with data load</name>
  <files>LabOutreachUI/Pages/Dictionary/ClientMaintenance.razor</files>
  <action>Create the page with `@page "/dictionary/clients"` and `@attribute [Authorize(Policy = "EditDictionary")]` (the policy from 13-01). Inject `DictionaryService`, `IUnitOfWork` (`LabBilling.Core.UnitOfWork.IUnitOfWork`), and `NavigationManager`. Add `<PageTitle>Client Maintenance</PageTitle>`. In `OnInitializedAsync`, load `allClients = await DictionaryService.GetAllClientsAsync(UnitOfWork, includeDeleted)` into a `List<Client>` field, wrapped in try/catch with `isLoading` and `errorMessage` fields exactly like ClientList.razor. Render a Bootstrap `table table-sm table-hover` inside a card with columns: Mnem, Name, Address (StreetAddress1), City, State, Zip, Facility No, Type (show `ClientType?.Description` ?? `Type` as string), Bill Method. Show a loading spinner/text while `isLoading`, and the `errorMessage` in an `alert alert-danger` when set. Grey deleted rows with a muted/`text-muted` row class when `IsDeleted`.</action>
  <verify>`dotnet build "LabOutreachUI/LabOutreachUI.csproj"` succeeds. Navigating to /dictionary/clients (as an authorized user) shows the table populated with clients.</verify>
  <done>Page compiles, is policy-protected, loads and displays the client grid with the listed columns; deleted rows visually distinct.</done>
</task>

<task type="auto">
  <name>Task 2: Add name/mnemonic filter and include-deleted toggle</name>
  <files>LabOutreachUI/Pages/Dictionary/ClientMaintenance.razor</files>
  <action>Add a filter text input (`@bind-Value` with `@bind-Value:event="oninput"` or an `oninput` handler) and an "Include inactive/deleted" checkbox above the table. Compute a `filteredClients` view from `allClients`: when filter text is non-empty, match `Name` containing the term (case-insensitive) OR `ClientMnem` equal to the term (case-insensitive). Bind the table to `filteredClients`. When the include-deleted checkbox changes, re-call the loader with the new `includeDeleted` value (server returns deleted records only when true), then re-apply the text filter. Debounce the text filter (~400-650ms) using a `System.Timers.Timer` or a `CancellationTokenSource`+`Task.Delay` pattern, and marshal UI updates with `InvokeAsync(StateHasChanged)`. Avoid filtering on every keystroke synchronously (the legacy form debounced 650ms to stay responsive on large client lists).</action>
  <verify>Typing a partial name filters the grid; typing an exact mnemonic finds that client; toggling "Include inactive" reloads and shows/hides deleted clients. Build succeeds.</verify>
  <done>Filter matches name-contains OR mnem-exact (case-insensitive), debounced; include-deleted reloads data; grid updates correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Add "New Client" button and per-row edit navigation</name>
  <files>LabOutreachUI/Pages/Dictionary/ClientMaintenance.razor</files>
  <action>Add a "New Client" button (top-right of the card header, `btn btn-primary` with `oi oi-plus`) that calls `NavigationManager.NavigateTo("dictionary/clients/new")`. Make each table row navigable to edit: add an Edit action per row (either a row `@onclick` or a trailing `btn btn-sm btn-outline-secondary` with `oi oi-pencil`) that calls `NavigationManager.NavigateTo($"dictionary/clients/{client.ClientMnem}")`. Use `ClientMnem` as the route key (it is the client's stable business key). Do not build the edit/new pages here — they are 13-03/13-04; these routes will 404 until then, which is expected.</action>
  <verify>Build succeeds. Clicking "New Client" navigates to /dictionary/clients/new; clicking a row/Edit navigates to /dictionary/clients/{mnem}. (Target pages not yet built — navigation target is what's verified.)</verify>
  <done>New button and per-row edit both navigate to the correct routes using ClientMnem.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Client Maintenance list page: grid, name/mnem filter, include-deleted toggle, New + per-row Edit navigation.</what-built>
  <how-to-verify>
    1. Run the app and log in as a dictionary editor.
    2. Click nav DICTIONARY → Clients. Confirm the grid loads with the listed columns.
    3. Type part of a client name — grid narrows. Clear it; type an exact mnemonic — that client shows.
    4. Toggle "Include inactive" — deleted clients appear (greyed) and disappear when unchecked.
    5. Click "New Client" — URL becomes /dictionary/clients/new (blank page for now is fine).
    6. Click a row's Edit — URL becomes /dictionary/clients/{mnem} (blank page for now is fine).
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `dotnet build "LabOutreachUI/LabOutreachUI.csproj"` succeeds.
- [ ] Page is gated by `[Authorize(Policy = "EditDictionary")]`.
- [ ] Filter (name-contains / mnem-exact) and include-deleted toggle work (verified in checkpoint).
- [ ] New and Edit navigations target the correct routes.
</verification>

<success_criteria>
- Client list page loads all clients via `DictionaryService.GetAllClientsAsync`.
- Filtering and include-deleted behavior match the legacy form's semantics.
- Navigation wired to `/dictionary/clients/new` and `/dictionary/clients/{mnem}`.
- No changes to Core/services required.
</success_criteria>

<output>
After completion, create `.planning/phases/13-dict-clients/13-02-SUMMARY.md`:

# Phase 13 Plan 02: Client List Page Summary

**[One-liner: browseable/filterable Client Maintenance list with add + edit navigation.]**

## Accomplishments
## Files Created/Modified
## Decisions Made
## Issues Encountered
## Next Step
Ready for 13-03-PLAN.md (Client edit form — core fields).
</output>
