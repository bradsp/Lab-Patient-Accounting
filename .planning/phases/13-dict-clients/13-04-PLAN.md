---
phase: 13-dict-clients
plan: 04
type: execute
---

<objective>
Complete the Client edit page: add the remaining sections (billing/printing Preferences, Medical
Review Officer, and Comments) and implement the NEW-client creation flow, including the mnemonic
duplicate check. After this plan, Client dictionary maintenance is feature-complete except for the
deferred Discounts/Interface-Mappings child grids (tracked as 13-05 in ISSUES.md).

Purpose: Reach practical parity with the legacy `ClientMaintenanceEditForm` for the fields users edit
day to day, and make "New Client" fully functional.
Output: Updated `Pages/Dictionary/ClientEdit.razor` with new sections + working add path.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/13-dict-clients/13-03-SUMMARY.md
@LabBilling Library/Models/Client.cs
@LabBilling Library/Models/Dictionaries.cs
@LabBilling Library/Services/DictionaryService.cs

**Key facts already verified (do not re-derive):**
- Preference fields on `Client` (all bool unless noted): `PrintInvoiceInDateOrder` (date_ord),
  `IncludeOnChargeCodeReport` (print_cc), `ShowDiscountedAmtOnBill` (bill_at_disc), `DoNotBill`
  (do_not_bill), `PrintCptOnInvoice` (prn_cpt4), and `DefaultDiscount` (per_disc, double — "Default Discount %").
  (Legacy also had a non-persisted "Collection Site on CoC" checkbox — skip it; it maps to no column.)
- MRO fields on `Client`: `MroName` (mro_name), `MroStreetAddress1` (mro_addr1), `MroStreetAddress2`
  (mro_addr2), `MroCity` (mro_city), `MroState` (mro_st — use `Dictionaries.StateSource`), `MroZipCode` (mro_zip).
- Comment field: `Comment` (comment, multiline).
- New-client flow (legacy): user types a mnemonic; on leaving the field it checks whether the mnemonic
  already exists via `GetClient`. If it exists → offer to edit that record instead. If not → proceed as
  a new `Client()` (Id == 0). `SaveClient` inserts when `Id == 0`.
- `DictionaryService.GetClient(mnem, uow)` returns null when the mnemonic does not exist.
- ClientEdit already routes `"new"` to a blank `Client()` (from 13-03 Task 1); this plan makes that path real.
- Required fields remain: ClientMnem, FeeSchedule, Type, GlCode, BillMethod (already enforced in 13-03).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Preferences, MRO, and Comments sections to the edit form</name>
  <files>LabOutreachUI/Pages/Dictionary/ClientEdit.razor</files>
  <action>Add three more sections (Bootstrap cards, or tabs if it reads better — match the existing form styling). **Preferences:** checkboxes bound to `PrintInvoiceInDateOrder` ("Print Bills in Date Order"), `IncludeOnChargeCodeReport` ("Include on Charge Code Report"), `ShowDiscountedAmtOnBill` ("Bill at Discount"), `DoNotBill` ("Do NOT Bill this Client"), `PrintCptOnInvoice` ("Print CPT on Invoice"); plus a numeric input for `DefaultDiscount` ("Default Discount %", `InputNumber<double>`). Skip the legacy non-persisted CoC checkbox. **MRO:** text inputs for `MroName`, `MroStreetAddress1`, `MroStreetAddress2`, `MroCity`, `MroState` (select from `Dictionaries.StateSource`), `MroZipCode`. **Comments:** an `InputTextArea` bound to `Comment`. All bind into the same `client` model used by the existing `EditForm`, so they save through the existing `SaveAsync`.</action>
  <verify>Build succeeds. Editing an existing client shows the preference checkboxes/discount, MRO fields, and comment populated; changing them and saving persists (re-open confirms).</verify>
  <done>Preferences, MRO, and Comments sections render, two-way bind to `client`, and persist via the existing save path.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the new-client flow with mnemonic duplicate check</name>
  <files>LabOutreachUI/Pages/Dictionary/ClientEdit.razor</files>
  <action>When the route is `"new"`: keep `ClientMnem` EDITABLE (it is read-only only for existing clients). When the user finishes entering the mnemonic (on `@onblur`/`@onchange` of the mnem input, or as a pre-save check inside `SaveAsync`), call `DictionaryService.GetClient(enteredMnem, UnitOfWork)`. If it returns non-null, block creation and surface a message like "Client {mnem} already exists" with a link/button to edit that existing record (`NavigationManager.NavigateTo($"dictionary/clients/{enteredMnem}")`) — mirror the legacy "edit this record instead?" behavior. If it returns null, proceed. On valid submit for a new client, ensure `client.Id == 0` so `SaveClient` inserts, call `DictionaryService.SaveClient(client, UnitOfWork)`, then navigate to the list (or to the newly-created client's edit page). Reuse the required-field validation from 13-03. Guard against double-submit.</action>
  <verify>Build succeeds. "New Client" → fill required fields with an UNUSED mnemonic → Save creates the client (it appears in the list). Entering an EXISTING mnemonic blocks creation and offers to edit the existing record.</verify>
  <done>New clients can be created with a unique mnemonic; duplicate mnemonic is detected and routes the user to the existing record; insert path uses `SaveClient` with `Id == 0`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Client edit form (Preferences/MRO/Comments) plus a working New-client flow with duplicate-mnemonic detection.</what-built>
  <how-to-verify>
    1. Run the app; log in as a dictionary editor; DICTIONARY → Clients.
    2. Edit an existing client. Confirm Preferences checkboxes, Default Discount %, MRO fields, and Comment
       are populated. Toggle a couple, change the comment, Save; re-open — changes persisted.
    3. Click "New Client". Enter an existing client's mnemonic — confirm it blocks and offers to edit that client.
    4. Enter a brand-new mnemonic, fill the required fields (name, fee schedule, client type, cost center,
       bill method), Save. Confirm the new client appears in the list and re-opens correctly.
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 13, or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Before declaring this plan (and Phase 13) complete:
- [ ] `dotnet build "LabOutreachUI/LabOutreachUI.csproj"` succeeds.
- [ ] Preferences/MRO/Comments load, edit, and persist (verified in checkpoint).
- [ ] New-client creation works and duplicate mnemonic is detected (verified in checkpoint).
- [ ] The full solution builds: `dotnet build "Lab Billing.sln" -p:Platform=x64`.
</verification>

<success_criteria>
- Client edit form reaches practical parity with the legacy edit dialog for all scalar/preference/MRO/comment fields.
- New and existing clients can both be saved; duplicate mnemonic handled.
- Phase 13 complete except the deferred Discounts + Interface Mappings grids (see ISSUES.md 13-05).
- No Core/service changes required.
</success_criteria>

<output>
After completion, create `.planning/phases/13-dict-clients/13-04-SUMMARY.md`:

# Phase 13 Plan 04: Preferences/MRO/Comments + New Client Summary

**[One-liner: Client dictionary maintenance feature-complete (minus deferred child grids).]**

## Accomplishments
## Files Created/Modified
## Decisions Made
## Issues Encountered
## Next Step
Phase 13 complete. Deferred: Discounts + Interface Mappings grids (ISSUES.md → 13-05). Update ROADMAP progress.
</output>
